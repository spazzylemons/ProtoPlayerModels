package spazzylemons.protoplayermodels

import net.minecraft.util.Identifier

const val MOD_ID = "protoplayermodels"

const val CONFIG_FOLDER = "config"
const val CONFIG_FILE = "config.properties"

val textureIdentifier = Identifier(MOD_ID, "proto")